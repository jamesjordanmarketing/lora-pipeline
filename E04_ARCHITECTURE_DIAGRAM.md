# Section E04: Architecture Diagram

**Training Execution & Monitoring - Complete System Flow**

---

## 🏗️ Full System Architecture

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                          SECTION E04 ARCHITECTURE                            │
│                     Training Execution & Monitoring                          │
└──────────────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│  USER INTERACTION LAYER                                                     │
└─────────────────────────────────────────────────────────────────────────────┘

    👤 User                      🖥️ Browser
     │                            │
     │  Navigate to Monitor      │
     └──────────────────────────►│  /training/jobs/[jobId]
                                 │
                                 │  Component: TrainingMonitorPage
                                 │  ├─ useTrainingJob (polling: 5s)
                                 │  ├─ useCancelJob
                                 │  └─ Recharts (loss curves)
                                 │
                                 │  Features:
                                 │  ├─ Progress bar (real-time)
                                 │  ├─ Metrics cards (loss, throughput, cost)
                                 │  ├─ Loss curve charts
                                 │  ├─ GPU utilization
                                 │  └─ Cancel button


┌─────────────────────────────────────────────────────────────────────────────┐
│  API LAYER (Next.js App Router)                                            │
└─────────────────────────────────────────────────────────────────────────────┘

    GET /api/jobs/[jobId]              POST /api/jobs/[jobId]/cancel
    ┌─────────────────────┐            ┌─────────────────────┐
    │  Job Details API    │            │  Cancel Job API     │
    │  ─────────────────  │            │  ─────────────────  │
    │  • requireAuth()    │            │  • requireAuth()    │
    │  • Fetch job        │            │  • Validate status  │
    │  • Fetch metrics    │            │  • Call GPU API     │
    │  • Fetch costs      │            │  • Update database  │
    │  • Enforce RLS      │            │  • Create notif     │
    └──────────┬──────────┘            └──────────┬──────────┘
               │                                  │
               │                                  │
               └──────────────┬───────────────────┘
                              │
                              ▼


┌─────────────────────────────────────────────────────────────────────────────┐
│  BACKGROUND PROCESSING LAYER (Supabase Edge Functions)                     │
└─────────────────────────────────────────────────────────────────────────────┘

                    ⏰ CRON TRIGGER: */30 * * * * *
                              (every 30 seconds)
                                     │
                                     ▼
                    ┌─────────────────────────────────┐
                    │   Edge Function                 │
                    │   process-training-jobs         │
                    │   ───────────────────────────   │
                    │   Runtime: Deno                 │
                    │   Auth: Service Role Key        │
                    │   Timeout: 60 seconds           │
                    └─────────────┬───────────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
                    ▼                           ▼
        ┌──────────────────────┐    ┌──────────────────────┐
        │  processQueuedJobs() │    │ updateRunningJobs()  │
        │  ──────────────────  │    │  ──────────────────  │
        │  1. Find queued      │    │  1. Find running     │
        │  2. Get signed URL   │    │  2. Poll GPU status  │
        │  3. Submit to GPU    │    │  3. Update progress  │
        │  4. Update status    │    │  4. Insert metrics   │
        │  5. Create notif     │    │  5. Calculate cost   │
        └──────────┬───────────┘    └──────────┬───────────┘
                   │                           │
                   └────────────┬──────────────┘
                                │
                                ▼


┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL SERVICES LAYER                                                    │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌────────────────────────────┐          ┌────────────────────────────┐
    │   GPU CLUSTER API          │          │   SUPABASE STORAGE         │
    │   ────────────────────     │          │   ────────────────────     │
    │   • Submit job             │          │   Buckets:                 │
    │   • Get status             │          │   ├─ training-files        │
    │   • Cancel job             │          │   │   (DATA-BRIDGE)        │
    │   • Receive metrics        │          │   └─ lora-datasets         │
    │   • Report completion      │          │       (E02 uploads)        │
    └────────────┬───────────────┘          └────────────┬───────────────┘
                 │                                       │
                 │  POST /training/submit                │  createSignedUrl()
                 │  GET /training/status/:id             │  (24-hour expiry)
                 │  POST /training/cancel/:id            │
                 │                                       │
                 └───────────────┬───────────────────────┘
                                 │
                                 ▼


┌─────────────────────────────────────────────────────────────────────────────┐
│  DATABASE LAYER (Supabase PostgreSQL with RLS)                             │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌───────────────────┐  ┌───────────────────┐  ┌───────────────────┐
    │  training_jobs    │  │  datasets         │  │  metrics_points   │
    │  ───────────────  │  │  ───────────────  │  │  ───────────────  │
    │  • id             │  │  • id             │  │  • job_id         │
    │  • status    ◄────┼──┼─ storage_bucket  │  │  • epoch          │
    │  • progress       │  │  • storage_path   │  │  • step           │
    │  • current_epoch  │  │  • training_ready │  │  • training_loss  │
    │  • current_step   │  │  • total_pairs    │  │  • validation_loss│
    │  • current_metrics│  └───────────────────┘  │  • throughput     │
    │  • current_cost   │                         │  • gpu_util       │
    │  • final_cost     │                         └───────────────────┘
    │  • external_job_id│
    │  • started_at     │
    │  • completed_at   │
    │  • error_message  │
    └───────────────────┘

    ┌───────────────────┐  ┌───────────────────┐
    │  cost_records     │  │  notifications    │
    │  ───────────────  │  │  ───────────────  │
    │  • job_id         │  │  • user_id        │
    │  • cost_type      │  │  • type           │
    │  • amount         │  │  • title          │
    │  • billing_period │  │  • message        │
    │  • details (JSON) │  │  • priority       │
    └───────────────────┘  └───────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│  DATA FLOW: Job Lifecycle                                                   │
└─────────────────────────────────────────────────────────────────────────────┘

    STATUS: queued
         │
         │  Edge Function picks up job
         │  ├─ Reads dataset.storage_bucket ('training-files' or 'lora-datasets')
         │  ├─ Generates signed URL from correct bucket
         │  └─ Submits to GPU cluster
         │
         ▼
    STATUS: initializing
         │
         │  GPU cluster starts job
         │  └─ Returns external_job_id
         │
         ▼
    STATUS: running
         │
         │  Edge Function polls GPU cluster (every 30s)
         │  ├─ Receives metrics (loss, throughput, GPU util)
         │  ├─ Inserts into metrics_points table
         │  ├─ Updates current_metrics, progress, current_epoch, current_step
         │  ├─ Calculates current_cost (hourlyRate × hoursElapsed)
         │  └─ Creates progress notifications
         │
         ▼
    STATUS: completed / failed / cancelled
         │
         │  Edge Function handles completion
         │  ├─ Sets final_cost = current_cost
         │  ├─ Inserts into cost_records table
         │  ├─ Creates completion notification
         │  └─ Records completed_at timestamp
         │
         ▼
    Ready for E05 (Model Artifacts)


┌─────────────────────────────────────────────────────────────────────────────┐
│  REAL-TIME UPDATE FLOW                                                      │
└─────────────────────────────────────────────────────────────────────────────┘

    Edge Function (30s)           Database                UI Polling (5s)
         │                           │                          │
         ├─ Update job status ──────►│                          │
         │                           │◄─────── Query job ───────┤
         │                           │                          │
         ├─ Insert metrics ─────────►│                          │
         │                           │◄─────── Query metrics ───┤
         │                           │                          │
         ├─ Calculate cost ─────────►│                          │
         │                           │◄─────── Query costs ─────┤
         │                           │                          │
         ├─ Create notification ────►│                          │
         │                           │◄─────── Query notifs ────┤
         │                           │                          │
         │                           │                          │
         │  [30 seconds later...]    │  [5 seconds later...]    │
         │                           │                          │
         └─► Repeat cycle            │          Repeat poll ◄───┘
                                     │
                                     └─► User sees real-time updates


┌─────────────────────────────────────────────────────────────────────────────┐
│  CRITICAL: DUAL STORAGE BUCKET SUPPORT                                      │
└─────────────────────────────────────────────────────────────────────────────┘

    Edge Function Logic:
    ────────────────────────────────────────────────────────────────────
    const storageBucket = job.dataset.storage_bucket || 'lora-datasets';

    const { data: signedUrlData } = await supabase.storage
      .from(storageBucket)  // ← Dynamic bucket selection!
      .createSignedUrl(job.dataset.storage_path, 3600 * 24);
    ────────────────────────────────────────────────────────────────────

    Supported Buckets:
    ┌─────────────────────────────────────────────────────────────────┐
    │  'training-files'                                               │
    │  └─ Files imported via DATA-BRIDGE section                      │
    │     └─ Example: datasets.storage_bucket = 'training-files'      │
    │                                                                 │
    │  'lora-datasets'                                                │
    │  └─ Files uploaded via E02 section                              │
    │     └─ Example: datasets.storage_bucket = 'lora-datasets'       │
    │        OR datasets.storage_bucket = NULL (defaults to this)     │
    └─────────────────────────────────────────────────────────────────┘

    Why This Matters:
    ├─ Seamless integration with DATA-BRIDGE (imported files)
    ├─ Backward compatible with E02 (uploaded files)
    ├─ Future-proof for additional storage backends
    └─ No hard-coded bucket assumptions


┌─────────────────────────────────────────────────────────────────────────────┐
│  ERROR HANDLING & RECOVERY                                                  │
└─────────────────────────────────────────────────────────────────────────────┘

    Edge Function Error Cases:
    ──────────────────────────────────────────────────────────────────
    ❌ Signed URL generation fails
       └─► Job marked as 'failed', notification created

    ❌ GPU cluster submission fails
       └─► Job marked as 'failed', error logged, notification created

    ❌ GPU cluster returns error during polling
       └─► Job marked as 'failed', error message saved

    ❌ Database update fails
       └─► Error logged, next cycle retries

    ✅ GPU cluster API temporarily unavailable
       └─► Polling continues, no status change until next successful poll
    ──────────────────────────────────────────────────────────────────

    User Cancellation Flow:
    ──────────────────────────────────────────────────────────────────
    1. User clicks "Cancel Job" on monitor page
    2. useCancelJob hook calls POST /api/jobs/[jobId]/cancel
    3. API validates job is cancellable (queued/initializing/running)
    4. API calls GPU cluster: POST /training/cancel/:external_job_id
    5. API updates database: status='cancelled', final_cost=current_cost
    6. API creates cancellation notification
    7. UI receives update via polling (5s max delay)
    8. Status badge changes to "Cancelled", polling stops
    ──────────────────────────────────────────────────────────────────


┌─────────────────────────────────────────────────────────────────────────────┐
│  PERFORMANCE CHARACTERISTICS                                                │
└─────────────────────────────────────────────────────────────────────────────┘

    Edge Function Performance:
    ├─ Execution time: < 2 seconds per cycle
    ├─ Trigger frequency: 30 seconds
    ├─ Batch size: 5 jobs per cycle
    ├─ Timeout: 60 seconds maximum
    └─ Concurrent jobs: Unlimited (processed in batches)

    API Performance:
    ├─ Job details endpoint: < 200ms response time
    ├─ Cancel endpoint: < 500ms response time
    ├─ Metrics query: Last 100 points (optimized)
    └─ RLS enforcement: Automatic user filtering

    UI Performance:
    ├─ Initial render: < 1 second
    ├─ Polling frequency: 5 seconds (active jobs only)
    ├─ Chart render: < 1 second (Recharts)
    └─ Auto-stop: Immediate on terminal state


┌─────────────────────────────────────────────────────────────────────────────┐
│  INTEGRATION WITH OTHER SECTIONS                                            │
└─────────────────────────────────────────────────────────────────────────────┘

    FROM E01 (Foundation):
    ├─ Database tables: training_jobs, datasets, metrics_points, etc.
    ├─ Types: TrainingJob, JobStatus, CurrentMetrics
    └─ Auth: requireAuth(), createServerSupabaseClient()

    FROM E02 (Dataset Management):
    ├─ Storage: Dataset files in lora-datasets bucket
    └─ Signed URLs: 24-hour access for GPU cluster

    FROM E03 (Training Configuration):
    ├─ Job records: Created with status='queued'
    ├─ Hooks: useTrainingJob (with polling), useTrainingJobs
    └─ Configuration: Hyperparameters, GPU config, cost estimates

    FROM DATA-BRIDGE (Training Files):
    ├─ Storage: Imported files in training-files bucket
    ├─ Dataset records: storage_bucket='training-files'
    └─ Dual support: Edge function handles both buckets

    TO E05 (Model Artifacts):
    ├─ Completed jobs: status='completed', artifact_id ready
    ├─ Job metadata: Training summary, hyperparameters
    ├─ Metrics: Historical data from metrics_points
    └─ Costs: Final costs in training_jobs.final_cost


┌─────────────────────────────────────────────────────────────────────────────┐
│  DEPLOYMENT TOPOLOGY                                                        │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────────────┐
    │  PRODUCTION ENVIRONMENT                                      │
    ├──────────────────────────────────────────────────────────────┤
    │                                                              │
    │  ┌────────────────────┐        ┌────────────────────┐       │
    │  │  Vercel            │        │  Supabase          │       │
    │  │  ────────────      │        │  ────────────      │       │
    │  │  • Next.js app     │◄──────►│  • PostgreSQL DB   │       │
    │  │  • API routes      │        │  • Edge Functions  │       │
    │  │  • UI pages        │        │  • Storage buckets │       │
    │  └────────────────────┘        │  • Cron scheduler  │       │
    │                                └────────────────────┘       │
    │                                         │                    │
    │                                         │                    │
    │                                         ▼                    │
    │                                ┌────────────────────┐       │
    │                                │  GPU Cluster       │       │
    │                                │  ────────────      │       │
    │                                │  • Training jobs   │       │
    │                                │  • Metrics API     │       │
    │                                │  • Status API      │       │
    │                                └────────────────────┘       │
    │                                                              │
    └──────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│  MONITORING & OBSERVABILITY                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

    Edge Function Logs:
    ├─ View in Supabase Dashboard → Edge Functions → Logs
    ├─ CLI: npx supabase functions logs process-training-jobs --follow
    └─ Includes: Job IDs, status transitions, errors, execution times

    Database Monitoring:
    ├─ SAOL testing script: node scripts/test-e04-database.js
    ├─ Active jobs count
    ├─ Metrics points count
    ├─ Cost records total
    └─ Notification types distribution

    UI Monitoring:
    ├─ Browser DevTools → Network tab (verify polling)
    ├─ React Query DevTools (query cache inspection)
    └─ Console logs (component lifecycle, errors)


┌─────────────────────────────────────────────────────────────────────────────┐
│  SECURITY CONSIDERATIONS                                                    │
└─────────────────────────────────────────────────────────────────────────────┘

    ✅ Service Role Key Usage:
       └─ Edge Function ONLY (full database access for job processing)

    ✅ RLS Enforcement:
       └─ API routes use user-scoped Supabase client (automatic filtering)

    ✅ User Ownership Validation:
       └─ All API routes verify user_id matches authenticated user

    ✅ Signed URL Security:
       └─ 24-hour expiry, dataset-specific, non-guessable tokens

    ✅ Environment Variables:
       └─ GPU cluster credentials stored securely in Supabase Dashboard


───────────────────────────────────────────────────────────────────────────────

                          📊 ARCHITECTURE SUMMARY

    Components:         8 files (4 production, 3 documentation, 1 test)
    External Services:  2 (GPU Cluster API, Supabase Storage)
    Database Tables:    5 (training_jobs, datasets, metrics_points, etc.)
    API Endpoints:      2 (job details, cancel)
    Edge Functions:     1 (process-training-jobs)
    UI Pages:           1 (training monitor)
    Polling Intervals:  2 (30s edge function, 5s UI)
    Storage Buckets:    2 (training-files, lora-datasets)

───────────────────────────────────────────────────────────────────────────────

                    ✅ SECTION E04 ARCHITECTURE COMPLETE

───────────────────────────────────────────────────────────────────────────────


